info:
  name: /api/notes_1
  type: http
  seq: 7

http:
  method: POST
  url: "{{noteHost}}/api/notes"
  body:
    type: json
    data: |2-
          {
          "idPatient": 4,
          "nomPatient": "TestPrécoce",
          "note": "TEST"
          }
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
