info:
  name: /api/notes (sans auth)
  type: http
  seq: 11

http:
  method: GET
  url: "{{noteHost}}/api/notes?idPatient=4"
  params:
    - name: idPatient
      value: "4"
      type: query
  auth:
    type: basic
    username: ""
    password: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
