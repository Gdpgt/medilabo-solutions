info:
  name: /api/patients
  type: http
  seq: 1

http:
  method: GET
  url: "{{patientHost}}/api/patients"
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
