info:
  name: /api/patients/{id}_1
  type: http
  seq: 4

http:
  method: PUT
  url: "{{patientHost}}/api/patients/5"
  body:
    type: json
    data: |-
      {
        "nom": "Travolta",
        "prenom": "John",
        "dateNaissance": "1950-12-31",
        "genre": "M",
        "adresse": "1 Oxford St",
        "telephone": "123-456-7890"
      }
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
