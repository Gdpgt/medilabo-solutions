info:
  name: /api/evaluation-risque/{idPatient} (sans auth)
  type: http
  seq: 12

http:
  method: GET
  url: "{{evaluationHost}}/api/evaluation-risque/4"
  auth:
    type: basic
    username: ""
    password: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
