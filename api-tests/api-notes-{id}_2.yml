info:
  name: /api/notes/{id}_2
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{noteHost}}/api/notes/69ff41ea923a41c6c3e26a49"
  auth:
    type: basic
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
